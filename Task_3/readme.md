# Task 3: SQL for Data Analysis

## 📌 Overview
This project is part of a **Data Analyst Internship – Task 3**, focused on using **SQL** to extract, manipulate, and analyze structured data from an e-commerce database.
The goal of this task is to demonstrate practical SQL skills such as filtering data, aggregations, joins, subqueries, views, and basic query optimization.

---

## 🗂 Dataset
- **Name:** Olist E-commerce Dataset
- **Format:** SQLite Database (`olist.sqlite`)
- **Domain:** E-commerce

The dataset contains information about customers, orders, payments, reviews, and related entities.

---

## 🛠 Tools & Technologies Used
- **SQLite**
- **DB Browser for SQLite**
- **GitHub** (for version control and submission)

---

## 📊 SQL Concepts Covered
The following SQL concepts were implemented as part of this task:

- `SELECT`, `WHERE`, `ORDER BY`
- `GROUP BY` with aggregate functions (`COUNT`, `AVG`, `SUM`)
- `INNER JOIN` and `LEFT JOIN`
- Subqueries
- Views (`CREATE VIEW`)
- Handling NULL values using `IFNULL`
- Query optimization using `INDEX`

---

## 📄 Files in This Repository
